#include "lists.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * insert_node - Used to insert needed node
 * @head: Entering point to linked list
 * @number: Number to insert
 * Return: Address of new node if successfull and  NULL if failed
 */
listint_t *insert_node(listint_t **head, int number)
{
	int c = NULL;
}
